# admin

The live owner admin, vault, stream, and ops dashboard remain in **`apps/web`**.

This folder exists so the empire layout has an `apps/admin` slot without relocating production routes (`/admin`, Pages Functions, Cloudflare Access).

Do not copy the website here.
