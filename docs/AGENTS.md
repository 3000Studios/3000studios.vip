# Agent catalog

Source of truth in code: `packages/studio-os/src/agents/registry.ts`

YAML/JSON copies live under `agents/*/AGENT.md` for humans.

Orchestrator assigns work. Agents do not call each other.
