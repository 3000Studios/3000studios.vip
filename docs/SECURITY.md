# Security

- Never commit `.env`, `global.env`, cookies, tokens, or private keys.
- `.env.example` lists names only for new Studio OS vars.
- Logs must not print secret values.
- DistroKid / paid ads / rights / destructive site ops require approval.
- Large media stays outside Git; store checksums and paths.
