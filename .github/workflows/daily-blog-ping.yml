# Scheduled blog ping removed. Manual only if ever needed.
name: Daily blog refresh

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  ping:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
